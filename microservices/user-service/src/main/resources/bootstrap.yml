cspring:
  cloud:
    config:
      uri: http://127.0.0.1:8888 # config 서버
      name: user-service # 읽어오기 위한 특정 파일명

# 우선 순위
# bootstrap -> application -> application-<name> > application-<name>-<profile>